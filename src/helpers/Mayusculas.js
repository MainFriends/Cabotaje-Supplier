const toUpperCase = (e) => {
    e.target.value = ( e.target.value +"").charAt(0).toUpperCase(0) + (e.target.value + "").slice(1).toLowerCase() ;
    
    }

        export {
            toUpperCase
            
        
        }


 
